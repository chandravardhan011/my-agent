version: "3.9"

services:
  avr:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./avr.db:/app/avr.db
      - ./avr.log:/app/avr.log
    restart: unless-stopped
